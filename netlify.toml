[build]
  command = "npm run build:netlify"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  NEXT_PUBLIC_RESEARCH_LAYERS = "false"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions.library]
  included_files = ["data/library/*.json"]
